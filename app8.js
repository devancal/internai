// Discovery 2.3: merge high-value Midwest employer feeds without changing the core feed contract.
const _loadAllEmployerJobs=loadLiveJobs;
loadLiveJobs=async function(force=false){await _loadAllEmployerJobs(force);try{const r=await fetch(`/api/midwest-jobs${force?`?t=${Date.now()}`:''}`,{cache:force?'no-store':'default'});if(!r.ok)throw new Error(`Midwest feed ${r.status}`);const d=await r.json(),extra=Array.isArray(d.jobs)?d.jobs:[];if(extra.length){const merged=[...extra,...jobsData],byUrl=new Map();for(const j of merged){const key=j.applyUrl||j.id;if(!byUrl.has(key))byUrl.set(key,j)}jobsData=[...byUrl.values()];jobsSource='live';if(state.page==='jobs')jobs();else if(state.page==='dashboard')dashboard()}}catch(e){console.warn('Midwest employer feed unavailable; core discovery feed remains active.',e)}};